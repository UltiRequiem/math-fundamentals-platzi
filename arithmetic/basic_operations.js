const BASIC_SYMBOLS = ["+", "-", "*", "/", "="];

const SUM = 5 + 2; // 7

const SUBTRACTION = 7 - 2; // 5
const MULTIPLICATION = 5 * 2; // 10
const DIVISION = Math.floor(11 / 2); // 5
const FLOAT_DIVISION = 11 / 2; // 5.5

console.log(SUM, SUBTRACTION, MULTIPLICATION, DIVISION, FLOAT_DIVISION);

console.log(`The basic mathematical symbols are ${BASIC_SYMBOLS} .`);
